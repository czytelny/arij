import React from 'react';
import Logo from '../logo'

const SideNav = () =>
  <div id="sidenav">
    <Logo/>
    <div className="sidenav-links">
      <h4>we are going to have links here!</h4>
    </div>
  </div>;

export default SideNav;
