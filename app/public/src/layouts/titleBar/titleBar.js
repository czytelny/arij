import React from 'react';

const TitleBar = () => (
  <div className="title-bar-container row">
    <div className="col-md-12 text-right">
      Title Bar Component
    </div>
  </div>
);

export default TitleBar;
