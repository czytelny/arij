"use strict";

const requests = {
  users: {
    get: "users:get",
    post: "users:post",
    post_resp: "users:post:response",
    put: "users:put",
    remove: "users:remove"
  }
};

module.exports = requests;
